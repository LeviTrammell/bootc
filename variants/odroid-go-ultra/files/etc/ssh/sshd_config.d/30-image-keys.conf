# Also accept keys baked into the immutable image (per-user file).
# ~/.ssh/authorized_keys (provisioned by bootc-image-builder from
# config.toml) still works — this is additive.
AuthorizedKeysFile .ssh/authorized_keys /usr/lib/ssh/%u.keys
