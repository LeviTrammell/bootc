[package]
name = "niri-nav"
version = "0.1.0"
edition = "2021"

[dependencies]
evdev = { version = "0.12", features = ["tokio"] }
tokio = { version = "1", features = ["full"] }
log = "0.4"
env_logger = "0.11"
anyhow = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
