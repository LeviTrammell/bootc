.DS_Store
dist
firmware

# Rust/cargo build output — workspace root and any per-crate target dirs
target/

# Large/flashable disk images (e.g. rocknix-fat.img) — never commit these
*.img

# Deprecated Mali proprietary GPU build output (project uses Panfrost now)
libmali-out/
