[package]
name = "cs-proto"
version = "0.1.0"
edition = "2021"
description = "Wire protocol + socket helpers shared between controller-shell, niri-nav, and any other compositor adapter."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
log = { workspace = true }
libc = { workspace = true }
